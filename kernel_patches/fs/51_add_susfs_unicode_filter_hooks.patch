diff --git a/fs/open.c b/fs/open.c
--- a/fs/open.c
+++ b/fs/open.c
@@ -35,6 +35,10 @@

 #include "internal.h"

+#ifdef CONFIG_KSU_SUSFS_UNICODE_FILTER
+#include <linux/susfs.h>
+#endif
+
 int do_truncate(struct user_namespace *mnt_userns, struct dentry *dentry,
 		loff_t length, unsigned int time_attrs, struct file *filp)
 {
@@ -1196,6 +1200,14 @@ static long do_sys_openat2(int dfd, const char __user *filename,
 	if (unlikely(open_flags != op->open_flag))
 		return -EINVAL;

+#ifdef CONFIG_KSU_SUSFS_UNICODE_FILTER
+	// Block Unicode bypass attacks on Android/data and Android/obb
+	if (susfs_check_unicode_bypass(filename)) {
+		return -ENOENT;
+	}
+#endif
+
 	fd = build_open_flags(op, &flags);
 	if (fd)
 		return fd;
diff --git a/fs/stat.c b/fs/stat.c
--- a/fs/stat.c
+++ b/fs/stat.c
@@ -20,6 +20,10 @@
 #include <linux/uaccess.h>
 #include <asm/unistd.h>

+#ifdef CONFIG_KSU_SUSFS_UNICODE_FILTER
+#include <linux/susfs.h>
+#endif
+
 #include "internal.h"
 #include "mount.h"

@@ -217,6 +221,14 @@ int vfs_fstatat(int dfd, const char __user *filename,
 {
 	int ret;

+#ifdef CONFIG_KSU_SUSFS_UNICODE_FILTER
+	// Block Unicode bypass attacks on Android/data and Android/obb
+	if (filename && susfs_check_unicode_bypass(filename)) {
+		return -ENOENT;
+	}
+#endif
+
 	if (flag & ~(AT_SYMLINK_NOFOLLOW | AT_NO_AUTOMOUNT | AT_EMPTY_PATH))
 		return -EINVAL;

